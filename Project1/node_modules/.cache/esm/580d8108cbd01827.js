let mongoose,config;_cbd‍.x([["default",()=>_cbd‍.o]]);_cbd‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_cbd‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MongoDB
const connectDatabase = async () => {
  try {
    await mongoose.connect(db, {
      useUnifiedTopology: true
    });
    console.log('Connected to MongoDB');
  } catch (error) {
    _cbd‍.g.console.error(error.message);

    // Exit with failure code
    process.exit(1);
  }
};

_cbd‍.d(connectDatabase);