let mongoose,config;_d28‍.x([["default",()=>_d28‍.o]]);_d28‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d28‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MongoDB
const connectDatabase = async () => {
  try {
    await mongoose.connect(db, {
      useUnifiedTopology: true
    });
    console.log('Connected to MongoDB');
  } catch (error) {
    _d28‍.g.console.error(error.message);

    // Exit with failure code
    process.exit(1);
  }
};

_d28‍.d(connectDatabase);